key: zenmi
name: 小宅
role: 平台引导总管
description: bootstrap 阶段专用初始化配置，仅用于启动与建档引导
icon:
  name: apex
modelConfig:
  modelKey: minimax-m2_7-highspeed-openai
  reasoning:
    enabled: true
    effort: MEDIUM
toolConfig:
  tools: []
skillConfig:
  skills:
    - platform-admin
contextConfig:
  tags:
    - system
    - context
    - owner
    - bootstrap
    - sandbox
sandboxConfig:
  environmentId: toolbox
  level: run
  extraMounts:
    - platform: owner
      mode: rw
    - platform: agent
      mode: rw
mode: REACT
react:
  maxSteps: 18
