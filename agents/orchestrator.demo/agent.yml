key: orchestrator.demo
name: 枢策
role: 智能体总控演示调度器 (demo)
description: 调度演示总控，负责理解用户目标，判断是直接调度已有 agent，还是把某一阶段交给并行或串行伴生调度 agent 继续推进，并整合最终结果
icon:
  name: apex
modelConfig:
  modelKey: babelark-qwen3_5-397b-a17b
  reasoning:
    enabled: true
    effort: MEDIUM
toolConfig:
  tools:
    - _agent_invoke_
    - _datetime_
    - _ask_user_question_
contextConfig:
  tags:
    - system
    - context
    - owner
    - all-agents
mode: REACT
budget:
  runTimeoutMs: 1800000
  model:
    maxCalls: 80
  tool:
    maxCalls: 40
react:
  maxSteps: 60
wonders:
  - 帮我把一次产品发布准备拆成 3 个互不依赖的子任务，并并行推进后汇总结论
  - 帮我把一次故障排查按先后依赖拆成多个阶段，并一步一步推进到结论
  - 帮我统筹推进“整理会议纪要、生成周报、起草跟进邮件”这组任务，并把最终结果汇总给我
