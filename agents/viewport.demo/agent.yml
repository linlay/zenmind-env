key: viewport.demo
name: 极光
role: 视图渲染示例 (demo)
description: 内置示例：REACT 模式按需多轮工具调用
icon:
  name: vertex
mode: REACT
modelConfig:
  modelKey: bailian-qwen3_5-plus
  reasoning:
    enabled: true
    effort: LOW
toolConfig:
  tools:
    - _datetime_
    - mock.weather.query
    - mock.logistics.status
    - mock.sensitive-data.detect
    - mock.todo.tasks.list
    - mock.transport.schedule.query
react:
  maxSteps: 6
wonders:
  - 帮我查今天上海的天气，并用 viewport 视图展示结果
  - 帮我查询顺丰运单 `SF1234567890` 的状态，并输出可渲染的视图块
  - 帮我把“提交周报、预约会议室、发送测试邮件”这 3 条待办整理成一张适合展示的卡片视图
