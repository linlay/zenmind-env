key: mock-create-expense
commands:
  - command: mock
    passThroughFlags: [--help, -h, --version]
    subcommands:
      - match: expense add
        level: 1
        title: "mock 费用报销"
        viewportType: html
        viewportKey: expense_form
